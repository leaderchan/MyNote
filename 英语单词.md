# 英语单词

(第一次整理2020年10月24日)

1. A-a:Arrays数组；阵列；一系列；，Argument实际参数，append附加；增补，Anchor link锚点链接，alert提醒；警告，alter改变性质；改变，arguments自变数；，agent代理人；，Asynchronous非同步的；不同时的；不同时存在的；异步的；不同速的，Anchor link锚点链接，assert断言；坚称；，ascending order 升序；升序排列；升序顺序，attribute属性；特性；archetype典型；原型；反复出现的象征；原始意象；原始
2. B-b:boolean布尔型,Bounds范围；界限；限制；，BufferedReader类 ; 缓冲流 ; 输入流 ; 缓冲区读取，border边界；边框，barter rabbet换行符，browser浏览者；浏览器，blur看不清的东西；模糊的东西；
3. C-c:const常数，console控制台，constructor构造，collection聚集；积聚；集中；，Client当事人；委托人；客户机；客户机；客服端，comments解释；注释；，checkbox复选框，container容器；集装箱，character字母；符号；，cause起因；原因；原则；事业；，context环境；背景；来龙去脉；上下文；语境，concatenate使连接（连续，衔接）起来；连锁；串级;
4. D-d:default系统默认值，debug调试，debuger调试器，demonstration示范；，daemon精灵；守护程序；，DatagramSocket构造数据报套接字，DatagramPacket数据包，division block-level划分块级，dispatcher调度员，dispatch派遣；发送；，dispatcher调度员；[计] 调度程序；[计] 分配器，drop卸；，descend依次递降，descending order 降序；递减顺序
5. E-e:enumerated枚举,Exception例外，异常，Extends继承，equals比得上；等于；，element要素；元素；，Even number偶数,easing减轻；放松；缓和；缓解；
6. F-f:format格式化，float浮点，filter过滤器，flush冲掉；，font字体，Form表单，function函数关系；函数，font字体，Form表单，Field字段; 范围；领域；视场，视野；foreign对外的；外地的；涉外的
7. G-g:generic广义的，普遍的；无显著特点的，get形成指明的状态；，Greater than大于，Globally search a Regular Expression and Print全局搜索正则表达式并打印
8. H-h:headline标题；重要消息；头条新闻；要闻，HypertextReference超文本引用，HypertextReference超文本引用
9. I-i:integer整型；整数，index索引；指示；标志，initialize初始化；格式化，implements实现；，iterator迭代器，illegalArgumentException非法参数异常，images图像；肖像；影像；印象；，interval间隔；间歇；，items项目；所有物品；，linear沿直线的；线形的；线性的；，images图像；肖像；影像；印象；，interval间隔；间歇；，Instance例子；事例；具体事件，invoke 求助于；援引；引用；产生；引起；造成；唤起；
10. J-j:Join连接；接合；
11. K-k:
12. L-l:List表，linkedlist链接列表，List items列表项，Less than小于
13. M-m:map映射，multiple多个的；多部分的；，mapping映射；映现，manual手册
14. N-n:NotSerializableException不可序列化异常，notation书面符号系统；注释；评注，navbar导航条
15. O-o:out of在……外 ; 离开，override覆盖；重写，Object物体；东西；对象；目标；，ordered整齐的;井井有条的;有序的，Ordered list有序列表，option选项，onclick单击事件 ; 点击 ; 单击 ; 鼠标单击 ; 点击事件，Odd number奇数，order by以...排序
16. P-p:print印刷，private私有的；独用的；保密的；隐蔽的；隔离的，parameters参数;决定因素，Property资，产；所有物；特性；性质；属性，printStackTrace打印栈跟踪轨迹，Protocol规程；约定；惯例；习惯做法；协议；，paragraph段落；，parse对（字符串；文本）作句法分析;解析，primary首要的；主要的；，post投寄；邮寄；，populate充满；出现于；输入，prefix前缀，Proxy代理权；代表权；替代值；投票委托书；代理人；受托人，prepend前置，placeholder占位符号pattern模式；方式；
17. Q-q:
18. R-r:return返回，reverse反转；颠倒；倒转；，row 一排；一行；一列；，radio单选，redis 队列 ; 配置说明 ; 配置方案;，recursive递归的
19. S-s:scan扫描，static静态的，split分裂；分离；，String串;线；，shuffle洗牌；改组；搅乱，Serializable可序列化，synchronized使同步；同步的；同时的，source来源；电源；出处；原始资料；源头；提供信息的人；源；，span全长；跨度；范围；，submit提交；呈递；，set设置，style风格；样式；，struct结构;结构体，StackOverflowError堆栈溢出；，swing摆动；摇动；
20. T-t:transient短暂的；暂时的；，Thread线；，throws扔；抛；，table列表；一览表；表格；，table data cell表格单元格，table head 表格头，type类型；种类；，text文本；，tag标签；，toggle开关；切换键；，tarball压缩包
21. U-u:unsigned无符号的，Unordered list无序列表，Uniform Resource Locator统一资源定位符，Uniform Resource Identifier通用资源标志符
22. V-v:void空的、无效的，Variable变量；可变的;
23. W-w:
24. X-x:
25. Y-y:
26. Z-z: